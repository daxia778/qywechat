# ── Rate Limiting Zones (http 上下文) ──
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;

upstream pdd_backend {
    server app:8201;
    keepalive 32;
}

# HTTP -> HTTPS 重定向
server {
    listen 80;
    server_name _;

    # 健康检查端点 (供 Docker/LB 使用，不强制 HTTPS)
    location /health {
        proxy_pass http://pdd_backend;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name _;

    # ── 隐藏 Nginx 版本号 ──
    server_tokens off;

    # ── TLS 证书 (部署时替换为真实证书路径) ──
    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    # ── TLS 安全配置 ──
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # ── 安全响应头 ──
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' wss://$host ws://$host; font-src 'self';" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # ── Gzip ──
    gzip on;
    gzip_types text/plain application/json application/javascript text/css text/xml;
    gzip_min_length 1000;

    # ── 请求限制 ──
    client_max_body_size 10m;

    # ── 静态资源 (带缓存) ──
    # 注意: Nginx 子 location 的 add_header 会覆盖父级，需通过 include 重新声明安全头
    location /assets/ {
        proxy_pass http://pdd_backend;
        expires 7d;
        add_header Cache-Control "public, immutable";
        add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' wss://$host ws://$host; font-src 'self';" always;
        include /etc/nginx/conf.d/security-headers.conf;
    }

    location /favicon.svg {
        proxy_pass http://pdd_backend;
        expires 7d;
        include /etc/nginx/conf.d/security-headers.conf;
    }

    location /icons.svg {
        proxy_pass http://pdd_backend;
        expires 7d;
        include /etc/nginx/conf.d/security-headers.conf;
    }

    # ── WebSocket ──
    location /api/v1/ws {
        proxy_pass http://pdd_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── 认证接口 (严格限速 + 请求体限制) ──
    location /api/v1/auth/ {
        limit_req zone=login burst=3 nodelay;
        client_max_body_size 64k;
        proxy_pass http://pdd_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }

    # ── API 反向代理 ──
    location /api/ {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://pdd_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_connect_timeout 10s;
        proxy_read_timeout 30s;
    }

    # ── 健康检查 ──
    location /health {
        proxy_pass http://pdd_backend;
        access_log off;
    }

    # ── 前端 SPA (兜底) ──
    location / {
        proxy_pass http://pdd_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── 禁止访问隐藏文件 ──
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
